# ⚠️ NEVER COMMIT THIS FILE WITH REAL VALUES! ⚠️
# 
# This is a template for .env.local (local development) or .env (production)
# Copy this file and fill in your actual secrets:
#
#   cp .env.local.example .env.local
#
# Both .env and .env.local are gitignored to protect your secrets.
#
# For production: Use environment variables or a secrets manager instead
# of committing an .env file.

# ============================================
# 🗄️ DATABASE CONFIGURATION
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pbj_db
DB_USER=pbj_user
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_HERE

# ============================================
# 🔐 JWT AUTHENTICATION
# ============================================
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_TO_RANDOM_SECRET_HERE
JWT_EXPIRE=7d

# ============================================
# 🌐 FRONTEND CONFIGURATION
# ============================================
FRONTEND_URL=http://localhost:9001
CORS_ORIGIN=http://localhost:9001

# ============================================
# 🤖 n8n WEBHOOK INTEGRATION
# ============================================
# IMPORTANT: See docs/API_SECURITY.md for webhook protection strategies
# 
# Option 1: Use backend proxy (recommended - n8n URL stays on backend)
# Set this on BACKEND only, not in frontend .env:
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/YOUR_WEBHOOK_ID_HERE
N8N_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET_HERE

# Option 2: Direct frontend to n8n (less secure - requires webhook auth)
# Only if you must expose n8n directly to frontend:
# VITE_N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/YOUR_WEBHOOK_ID_HERE
# VITE_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET_HERE

# ============================================
# 🔑 API KEYS (if using third-party APIs)
# ============================================
# Add your API keys here as needed:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# REPLICATE_API_TOKEN=r8_...

# ============================================
# 🚀 SERVER CONFIGURATION
# ============================================
NODE_ENV=development
PORT=5000
API_BASE_URL=http://localhost:5000
API_RATE_LIMIT=100

# ============================================
# 📦 STORAGE (for production)
# ============================================
STORAGE_PATH=/app/storage
BRAND_KITS_DIR=/app/storage/brand_kits
SCHEDULED_POSTS_DIR=/app/storage/scheduled_posts
MEDIA_DIR=/app/storage/media
GENERATED_HISTORY_DIR=/app/storage/generated_history
ARCHIVE_DIR=/app/storage/archive

# ============================================
# 🔴 REDIS (for production queue/cache)
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================
# 👷 WORKER (for background jobs)
# ============================================
WORKER_CONCURRENCY=5
